/**
 * Liquid-bubble surface tokens shared by Card, Money tiles, and CTAs.
 * Prefer the CSS class `liquid-bubble` for theme-aware fill/border; these
 * Tailwind strings are for one-off interactive surfaces that already use Card.
 */
export const LIQUID_BUBBLE =
  "liquid-bubble rounded-[28px] border border-transparent shadow-[0_8px_32px_-12px_rgba(0,0,0,0.45)]";

/** Active pill color for the Money tab (matches AppShell NAV_ITEMS Money). */
export const MONEY_ACCENT = "#22c55e";
